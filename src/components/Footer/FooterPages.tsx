import { Box, Typography } from '@mui/material';

import './Footer.css';

const FooterPages = () => {
  return (
    <Box id='footer-pages' className='footer-box'>
      <Typography className='footer-box-header' variant='h5'>Strony</Typography>
    </Box>
  )
}

export default FooterPages;